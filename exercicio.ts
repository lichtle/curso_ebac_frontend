function multiplicar(x: number, y: number): number {
  return x * y;
}

console.log(multiplicar(2, 4));

function saudar(nome: string): string {
  return `Olá ${nome}!`;
}

console.log(saudar("Lichtle"));
